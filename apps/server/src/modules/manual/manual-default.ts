/**
 * 使用手册默认内容（Markdown）。
 *
 * 首次启动由 seed.ts 以 INSERT OR IGNORE 写入 manuals 表；此后由管理员在
 * 「使用手册」页编辑，内容以数据库为准，代码里的这份只在缺行时作为兜底。
 *
 * 图片约定：正文里用 `![图片说明](placeholder)` 声明占位图，前端渲染为
 * 「待补充图片」提示框并展示说明文字，管理员按说明补图即可，不影响排版。
 */

export const MANUAL_DEFAULT_SLUG = 'user-guide';
export const MANUAL_DEFAULT_TITLE = '科应共享账号管理平台 · 使用手册';

export const MANUAL_DEFAULT_CONTENT = `# 科应共享账号管理平台 · 使用手册

> 适用对象：全体使用共享账号的同事（第 1~4 章）与平台管理员（第 5 章）
> 阅读时长：同事约 8 分钟；首次使用请务必完成第四章的「安装校验」

---

## 目录

- [1. 平台是什么](#1-平台是什么)
- [2. 同事快速上手](#2-同事快速上手)
- [3. 浏览器助手（扩展）安装](#3-浏览器助手扩展安装)
- [4. 安装校验与自检](#4-安装校验与自检)
- [5. 管理员指南](#5-管理员指南)
- [6. 常见问题排查（FAQ）](#6-常见问题排查faq)
- [7. 安全与隐私说明](#7-安全与隐私说明)
- [8. 速查表](#8-速查表)

---

## 1. 平台是什么

平台把单位采购的**科应（scienceing.com）共享账号**做成了一个「借用—归还」的池子：你用时领取，用完归还，下一位同事接着用。系统会自动改密、自动回收，你不需要知道账号密码。

一句话概括流程：**登录看板 → 领取账号 → 打开科应（自动登录）→ 用完归还。**

![平台流程示意：看板 → 领取 → 科应 → 归还 四步闭环](placeholder)

### 1.1 三个角色

| 角色 | 能做什么 |
|---|---|
| 普通用户 | 领取账号、使用、归还；查看「我的账号」 |
| 管理员 | 上述全部 + 账号/用户管理、强制回收、改密、看数据看板、编辑本手册 |
| 游客 | 只能看首页账号余量与使用手册（未登录） |

### 1.2 关键规则（请务必知晓）

1. **一人一号**：同一时间你只能持有 1 个账号，一个账号也只会被 1 人持有。
2. **无操作自动回收**：超过设定时长（默认 30 分钟）没有任何真实操作，系统判定你已离开，自动回收账号并改密。在页面里点击、滚动、打字都会续期。
3. **用完请主动归还**：主动归还最快，也让同事少等。
4. **密码不对你可见**：账号密码由系统加密保存并自动改密，页面只提供「打开科应」自动登录，不展示密码。

---

## 2. 同事快速上手

### 2.1 登录

在浏览器打开管理员分发的内网看板地址（形如 \`http://10.x.x.x:18080/\`），输入账号密码登录。

![登录页：用户名、密码输入框与「登录」按钮](placeholder)

> 登录不上？确认账号是否已由管理员创建、密码是否正确；连续失败请联系管理员，不要自行反复尝试。

### 2.2 领取账号

登录后进入首页，顶部显示当前**可用账号数量**。点击 **「领取账号」**，系统会为你分配一个当前空闲的账号。

![看板首页：账号余量统计与「领取账号」按钮](placeholder)

若提示「暂无可用账号」，说明账号都被占用了，稍等几分钟再试——归还后账号需要几十秒自动改密才会重新变为可用。

### 2.3 打开科应

领取成功后进入「我的账号」页，卡片上会显示：账号名称、已用时长、**剩余租期倒计时**。点击 **「打开科应」**，浏览器会自动新开标签页并**自动完成登录**。

![我的账号页：账号卡片、倒计时与「打开科应」「立即归还」按钮](placeholder)

> 如果点「打开科应」只打开了登录页而没有自动登录，说明浏览器助手没装好或没生效，请直接跳到[第 3 章](#3-浏览器助手扩展安装)。

### 2.4 使用期间

- 保持页面真实操作（点击、滚动、输入）即可自动续期，无需做任何额外动作。
- 科应页面右下角有**倒计时悬浮窗**，随时告诉你还剩多久（颜色含义见 [4.2](#42-悬浮窗状态对照表)）。
- 剩余 1 分钟会弹窗提醒：继续用点「继续使用」，用完了点「立即归还」。

### 2.5 归还账号

两种方式，效果相同：

1. **主动归还**：在「我的账号」页或科应页右下角悬浮窗点 **「立即归还」**。
2. **超时自动归还**：无操作达到设定时长后系统自动回收。

归还后系统会**自动改密**，账号回到「回收中」，约几十秒后重新变为「可用」。

![归还流程：点击立即归还 → 账号进入回收中 → 自动改密 → 重新可用](placeholder)

---

## 3. 浏览器助手（扩展）安装

> **不装助手会怎样？** 能正常登录看板、能看到余量，但**无法领取账号**、无法自动续期、科应页也不会出现倒计时。想正常使用必须安装。

助手负责四件事：**自动绑定账号、自动续期、租期倒计时提醒、快速归还**。

### 3.1 环境要求

- 浏览器：**Chrome** 或 **Edge** 桌面版，版本 102 及以上（近几年装的都满足）。
- 手机浏览器、IE、360 兼容模式**不支持**。

### 3.2 下载安装包

登录看板后，任选一种方式下载扩展压缩包（\`scienceing-extension.zip\`）：

- **方式 A（推荐）**：看板**顶栏**的助手状态条右侧 **下载图标**；
- **方式 B**：「我的账号」页提示「未检测到助手」时的 **下载助手 ZIP** 链接；
- **方式 C**：管理后台 →「系统参数」→「扩展配置」→ **下载最新版 ZIP**。

不知道下载到哪了？按 \`Ctrl + J\` 打开浏览器的下载列表查看。

![下载入口：顶栏助手状态条与下载图标特写](placeholder)

### 3.3 解压（关键一步，别跳过）

1. 在电脑上新建一个**固定文件夹**，例如 \`C:\\ScienceingAssistant\`（**不要**放在「下载」文件夹里）。
2. 把 ZIP 右键 → **全部解压缩** 到这个文件夹。
3. 确认 \`manifest.json\` **直接**在该文件夹里：
   - 正确：\`C:\\ScienceingAssistant\\manifest.json\`
   - 错误：\`C:\\ScienceingAssistant\\科应共享账号助手_v1.3.0\\manifest.json\`

![解压后的目录结构：manifest.json 位于根目录，而不是多套一层子文件夹](placeholder)

> **三条铁律**
> 1. 这个文件夹要**一直保留**，不要删除、移动、改名——插件是"加载已解压扩展"方式运行的，随时读取它。删了插件立即失效。
> 2. **不要把 ZIP 直接拖进浏览器**，Chrome / Edge 已不支持拖拽安装。
> 3. 换电脑、换浏览器需要重新装一次。

### 3.4 在 Chrome 中安装

1. 地址栏输入 \`chrome://extensions\` 回车，进入扩展管理页；
2. 打开页面**右上角**的 **「开发者模式」** 开关；
3. 点击**左上角** **「加载已解压的扩展程序」**；
4. 选中 3.3 里解压好的文件夹 → **「选择文件夹」**；
5. 列表出现 **「科应共享账号助手」** 卡片（带版本号）且**无红色报错**，即安装成功。

![Chrome 扩展管理页：右上角开发者模式开关 + 左上角「加载已解压的扩展程序」按钮](placeholder)

### 3.5 在 Edge 中安装

1. 地址栏输入 \`edge://extensions\` 回车；
2. 打开页面**左下角**的 **「开发人员模式」** 开关；
3. 点击 **「加载解压缩的扩展」**（Edge 叫"解压缩"，与 Chrome 的"已解压"是同一个意思）；
4. 选中解压文件夹 → **「选择文件夹」**；
5. 出现「科应共享账号助手」卡片且无红色报错即成功。

![Edge 扩展管理页：左下角开发人员模式开关 + 「加载解压缩的扩展」按钮](placeholder)

> 若提示 \`Manifest file is missing or unreadable\`，是选错了文件夹层级，见 [6.1](#61-加载时报错清单文件缺失manifest-file-is-missing-or-unreadable)。

---

## 4. 安装校验与自检

装完请完整走一遍下面 5 步，全部通过才算可用。

### 4.1 五步冒烟测试

| 步骤 | 操作 | 通过标准 |
|---|---|---|
| ① 固定图标（可选） | 点右上角拼图图标 🧩 → 找到助手 → 点图钉 📌 | 工具栏出现助手图标 |
| ② 看板握手 | 刷新看板页面，看**顶栏状态条** | 显示绿色「助手已就绪 · vX.Y.Z」（约 3 秒内自动完成） |
| ③ 自动打开 | 「我的账号」点「领取」→ 点「打开科应」 | 自动新开科应页并完成登录，无需手输密码 |
| ④ 悬浮窗 | 看科应页面**右下角** | 出现圆形倒计时环（绿色 + 释放时间） |
| ⑤ 续期与归还 | 在科应页点几下 / 滚一滚 → 再点「立即归还」 | 时间回到满格；点归还后跳回看板并释放账号 |

![安装成功标志：扩展列表出现「科应共享账号助手」卡片，无红色报错](placeholder)

### 4.2 悬浮窗状态对照表

| 颜色 / 文案 | 含义 | 该怎么做 |
|---|---|---|
| 🟢 绿色（5~30 分钟） | 正常 | 无需操作 |
| 🟡 黄色（1~5 分钟） | 即将到期 | 继续真实操作即自动续期 |
| 🔴 红色（不足 1 分钟） | 马上到期 | 点「继续使用」或「立即归还」 |
| 🟠 琥珀色「异常」 | 连不上后端 | 检查是否在内网、能否打开看板，稍后自动恢复 |
| ⚪ 灰色「未绑」 | 当前没绑定账号 | 回看板领取账号 |
| ⚪ 灰色「已释放」 | 租期已结束 | 点「返回看板」重新领取 |

![科应页面右下角倒计时悬浮窗：绿色圆环 + 剩余时间 + 立即归还按钮](placeholder)

### 4.3 命令行级别的自检（进阶）

若上面任一步不通过，可打开浏览器开发者工具（\`F12\`）→ Console，查看是否有红色报错并截图发给管理员。

管理员侧可在「系统参数 → 自动化健康检查」执行一次健康检查，确认科应后台可登录、账号页面可访问、重置入口正常。

---

## 5. 管理员指南

### 5.1 账号管理（管理后台 → 账号管理）

- 新增 / 重命名 / 禁用 / 删除科应账号；支持 **CSV 批量导入**。
- **强制回收**：把被占用的账号强制收回并改密（员工忘记归还时用）。
- **重置密码**：手动触发一次改密，用于修复异常账号。
- **标记可用**：账号卡在异常状态时手工恢复。

### 5.2 用户管理（管理后台 → 用户管理）

- 新增 / 编辑 / 停用用户，支持 CSV 批量导入；可重置用户密码。

### 5.3 数据看板（管理后台 → 数据看板）

可视化查看：账号状态分布、账号负载 TOP10、密码健康度与异常清单、改密任务成功率、近 30 天领用趋势、用户活跃排行、释放原因构成。右上角可切换 7/30/90 天。

![管理后台数据看板：KPI 卡片 + 环形图 + 趋势折线 + 排行榜](placeholder)

### 5.4 系统参数（管理后台 → 系统参数）

- **无操作超时（分钟）**：默认 30，超时自动回收。
- **提醒阈值**：一级提醒 300 秒、二级（紧急）提醒 60 秒。
- **扩展配置**：最低版本、最新版本号，以及**打包分发给同事的 ZIP 下载**。

> 内网 IP 变更后必须重新执行打包（\`node deploy-lan/scripts/deploy.mjs extension:pack\`），否则旧安装包匹配不上新地址。

---

## 6. 常见问题排查（FAQ）

### 6.1 加载时报错「清单文件缺失」（Manifest file is missing or unreadable）

**原因**：选错了文件夹层级，多半是解压后多套了一层。
**解决**：往里看一层，改选**直接包含 manifest.json 的那个文件夹**，或重新解压后再选。

### 6.2 把 ZIP 拖进浏览器没反应

Chrome / Edge 出于安全已**不支持**拖拽 \`.zip\` / \`.crx\` 安装。请按 [3.4](#34-在-chrome-中安装) / [3.5](#35-在-edge-中安装) 的「开发者模式 → 加载已解压的扩展程序」流程安装。

### 6.3 插件装好了，看板仍提示「未检测到助手」

按顺序排查：

1. **刷新看板页面**（\`F5\`）——握手约 3 秒，刷新前看到的是旧状态；
2. **确认看板地址没变**——内网 IP 变更后旧安装包失效，需让管理员重新打包并重新下载安装；
3. **确认解压文件夹还在**——被删掉/移动过就重装；
4. **无痕窗口用户**：扩展页 → 卡片「详细信息」→ 打开「在无痕模式下启用」（Edge 为「InPrivate 中允许」）；
5. **多用户（Profile）**：确认装在了当前正在用的浏览器用户下。

### 6.4 提示「版本过旧 / 需要更新」

重新下载最新 ZIP → **覆盖解压**到原文件夹 → 扩展页点卡片上的 **↻ 重新加载** → 回看板刷新。

### 6.5 打开科应后右下角没有悬浮窗

1. 确认当前确实是 \`www.scienceing.com\`（助手只在看板与科应两个站点工作）；
2. 确认**已经领取**账号（未绑定时显示灰色"未绑"）；
3. 确认扩展卡片处于**启用**状态（灰色就点一下）；
4. 仍不行：\`F12\` → Console 截图报错发给管理员。

### 6.6 橙色「异常」/ 一直连不上后端

说明插件连不上内网看板：检查网络、能否打开看板页面。网络恢复后会自动重连，也可点悬浮窗「返回看板」重新触发。

### 6.7 浏览器一直提示「请停用以开发者模式运行的扩展程序」

**属正常现象**，点「取消」即可，不影响使用。本插件为单位内网分发，无法上架官方商店，只能以开发者模式加载。

### 6.8 公司电脑提示「已被管理员禁用扩展安装」

部分企业电脑用组策略禁用了开发者模式。请把本文档连同安装包发给 IT / 管理员统一处理。

### 6.9 换了电脑 / 换浏览器要重装吗

要。新设备只需：下载 ZIP → 解压 → 加载一次，无需其他配置。

---

## 7. 安全与隐私说明

助手遵循**最小权限**原则：

- 仅申请 2 项权限：\`tabs\`（识别科应页从哪个标签打开，用于自动绑定）、\`storage\`（会话级保存本次租期标识，关浏览器即清空）；
- 不申请 \`cookies\`、\`history\`、\`webRequest\` 等任何多余权限；
- **绝不采集**搜索词、论文正文、输入内容、Cookie、科应密码或管理员凭据；
- 只在你**持有账号期间**工作，其余时间基本待机。

平台侧：账号密码以主密钥加密存储（AES-256-GCM），归还后自动改密，全程不向使用者展示明文密码；所有关键操作（登录、领取、归还、改密、强制回收、参数变更）都会记入**系统日志**可追溯。

---

## 8. 速查表

| 事项 | 速查 |
|---|---|
| Chrome 扩展管理页 | 地址栏 \`chrome://extensions\` |
| Edge 扩展管理页 | 地址栏 \`edge://extensions\` |
| 安装方式 | 开发者模式 → 加载已解压的扩展程序 → 选含 \`manifest.json\` 的文件夹 |
| 安装成功标志 | 扩展列表出现「科应共享账号助手」卡片且无红色报错 |
| 激活成功标志 | 看板顶栏状态「助手已就绪」；点「打开科应」后右下角出现倒计时圆环 |
| 升级方法 | 重下 ZIP → 覆盖解压 → 扩展卡片点 ↻ 重新加载 |
| 无操作超时 | 默认 30 分钟（管理员可改），真实操作即自动续期 |
| 常见安装报错 | \`Manifest file is missing or unreadable\` = 选错文件夹层级 |

---

*本手册由管理员维护，内容随平台版本更新。技术细节（打包、域名配置、握手协议）见仓库 \`docs/install-extension.md\` 与 \`apps/extension/README.md\`。*
`;
