# 科应共享账号管理平台 — 产品设计计划

> **Scienceing Shared Account Manager · Product Design Plan**
>
> 依据文档：`PRD.md`（产品需求，v1）｜`DESIGN.md`（视觉规范）｜`theme.css`（设计令牌，Tailwind v4）
>
> 状态：v1.1 —— 已确认状态标签引入语义色（§4.1），供评审后进入实现

---

## 0. 文档说明

### 0.1 输入

| 输入 | 作用 |
|---|---|
| `PRD.md`（2902 行） | 功能需求、状态机、业务流程、路由、验收场景的唯一事实来源 |
| `DESIGN.md` | 视觉语言：无彩色系统、Geist 字阶、18/24px 圆角体系、徽章与按钮规范 |
| `theme.css` | 可直接落地的 Tailwind v4 `@theme` 令牌（颜色/字号/圆角/阴影） |

### 0.2 产出

本计划只做**产品与界面设计**，不写代码。交付内容：

1. 设计目标与原则（§2）
2. 设计令牌落地方式，含 shadcn/ui 主题映射与中文排版适配（§3）
3. 信息架构与角色视图（§4）
4. **状态视觉语言** —— 本产品最核心的设计决策（§5）
5. 全部页面的设计规格：布局线框、组件清单、状态清单（§6）
6. 组件规格与 shadcn/ui 映射（§7）
7. 浏览器扩展悬浮窗设计（§8）
8. 关键交互流程（§9）
9. 响应式 / 可访问性 / 动效（§10）
10. 设计验收清单（对应 PRD §60 十个验收场景）（§11）
11. 分阶段落地计划（§12）
12. 风险与待确认（§13）

### 0.3 设计立场

PRD 62 已定性：这不是"在线状态监控平台"，而是**租约管理与自动回收基础设施**。因此界面气质应接近开发者工具（Vercel / Linear 一脉）——**安静、克制、信息密度高、状态永远可读**，而不是消费级产品。所有视觉决策以此为第一原则。

---

## 1. 设计目标与原则

### 1.1 目标

| # | 目标 | 对应 PRD |
|---|---|---|
| G1 | 任何人 3 秒内看懂"账号池现在什么情况" | §2.1 / §35 |
| G2 | 领取、归还、续期三个动作零歧义、零误触 | §12 / §14 / §32 |
| G3 | 状态表达不依赖颜色也能读懂（色盲安全） | §5 本计划 |
| G4 | 管理后台支持 10 账号规模下的快速运维 | §37 |
| G5 | 悬浮窗在科应页面内不违和、不打扰、可折叠 | §20 / §21 |

### 1.2 设计原则（从 DESIGN.md 三条哲学 + PRD 约束推导）

- **P1 · 无色即秩序（状态标签例外）**：整个系统以黑白灰为主，ember 红仅出现在「错误 / 破坏性确认 / 失败结果」。**唯一例外**：状态标签（圆点 + 徽章）允许语义色（绿/蓝/琥珀/红），边界见 §4.1——彩色绝不进入按钮、卡片、导航、正文。
- **P2 · 半径即层级**：交互元素 18px（按钮/输入/徽章/悬浮窗），容器 24px（卡片/弹窗/侧边栏项例外），全系统不出现第三种圆角。
- **P3 · 静默悬浮**：卡片阴影 = 1px hairline + 1px/2px 微偏移，观感是"浮起来但没戏剧性"。
- **P4 · 状态先于一切**：账号/租约/插件/自动化四类状态，一律"**圆点 + 文本标签**"成对出现，文本是主通道，圆点是辅助。
- **P5 · 工具型密度**：紧凑密度（4px 基准），单屏信息量优先，不做大留白营销页。
- **P6 · 时间即语义**：倒计时、无操作时长是产品核心信息，用等宽数字（tabular-nums）、统一的相对时间格式，全站一致。
- **P7 · 异常前置**：插件未装、版本过旧、回收失败、网络异常——都在用户**动手之前**可见，而不是动手之后报错。

---

## 2. 设计令牌落地

### 2.1 直接复用 `theme.css`

`theme.css` 已提供 Tailwind v4 `@theme` 令牌，实现阶段原样引入，禁止修改色值。语义命名即用途：

```css
@theme {
  --color-canvas: #f5f5f5;      /* 页面底 */
  --color-paper: #ffffff;       /* 卡片面 */
  --color-surface-alt: #fafafa; /* 侧边栏/输入休止 */
  --color-ink: #0a0a0a;         /* 主文本/主按钮 */
  --color-ink-soft: #171717;    /* 实心徽章底 */
  --color-mid-gray: #737373;    /* 次要文本 */
  --color-hairline: #e5e5e5;    /* 描边 */
  --color-ember: #e7000b;       /* 仅错误/破坏性 */

  /* 状态语义色 —— 唯一允许彩色的元素：状态圆点/状态徽章（§4.1） */
  --color-status-available: #16a34a;
  --color-status-available-soft: #f0fdf4;
  --color-status-in-use: #2563eb;
  --color-status-in-use-soft: #eff6ff;
  --color-status-recycling: #d97706;
  --color-status-recycling-soft: #fffbeb;
  --color-status-error: #e7000b;
  --color-status-error-soft: #fdecec;
}
```

> `theme.css` 已同步加入上述状态语义色令牌（v1.1），Tailwind 可直接使用 `bg-status-*` / `text-status-*` 类。

### 2.2 shadcn/ui 主题映射（实现用）

PRD §7.6 约定 Tailwind + shadcn/ui。把 shadcn 的 CSS 变量映射到本系统令牌，一处生效全局：

| shadcn 变量 | 值 | 说明 |
|---|---|---|
| `--background` | `#f5f5f5` | canvas |
| `--foreground` | `#0a0a0a` | ink |
| `--card` / `--popover` | `#ffffff` | paper |
| `--card-foreground` / `--popover-foreground` | `#0a0a0a` | ink |
| `--primary` / `--primary-foreground` | `#0a0a0a` / `#fafafa` | 实心主按钮（唯一高强调交互） |
| `--secondary` / `--secondary-foreground` | `#f5f5f5` / `#0a0a0a` | ghost 按钮 |
| `--muted` / `--muted-foreground` | `#f5f5f5` / `#737373` | 弱化面/次要文本 |
| `--accent` / `--accent-foreground` | `#fafafa` / `#171717` | 悬浮高亮 |
| `--destructive` / `--destructive-foreground` | `#e7000b` / `#ffffff` | 仅错误与破坏性 |
| `--border` | `#e5e5e5` | hairline |
| `--input` | `#f5f5f5` | 输入休止填充 |
| `--ring` | `#e5e5e5` | 聚焦环 |
| `--radius` | `18px` | 交互元素默认 |
| `--radius-cards` | `24px` | 容器卡片（自定义变量） |

> 卡片圆角为 24px，与交互元素 18px 区分：shadcn `Card` 组件通过 `rounded-[24px]` 或自定义 `--radius-cards` 显式覆盖。

### 2.3 中文排版适配（Geist 不含中文字形）

Geist 仅覆盖拉丁字符，中文必须回退。字体栈与字阶规则：

```css
--font-sans: 'Geist', 'Noto Sans SC', 'PingFang SC', 'Microsoft YaHei',
             ui-sans-serif, system-ui, sans-serif;
```

| 场景 | 规则 |
|---|---|
| 中文正文 | 14px/400，行高 1.43–1.56（中文需比拉丁略宽松） |
| 中文标题 | 24–36px/600；**中文 48px 显示级字号不建议使用**（视觉过重），首页大数字仍用 48px（数字由 Geist 渲染） |
| 中文标题字距 | 不套用 -0.05em 紧排（中文无字距可紧），一律 0 |
| 等宽数字 | 倒计时/统计数字加 `font-feature-settings: "tnum"`（tabular-nums），跳动不抖动 |
| caption | 12px 大写仅适用于英文标签；中文标签用 12px/500 + `#737373`，不做大写变换 |

### 2.4 字体加载

Vite 前端通过 `@fontsource/geist-sans`（或自托管 woff2）引入 Geist 400/500/600；无需引入中文 web 字体，回退到系统中文即可（内网工具，避免额外体积）。

---

## 3. 信息架构与角色视图

### 3.1 页面地图（对齐 PRD §38）

```text
/
    公开账号池看板（游客可看，登录后含领取入口）

/login
    登录

/my
    我的当前租约（无租约 = 空态 + 领取入口）

/admin            （管理员专属，非管理员访问显示 403 空态）
├── /admin/accounts   科应账号管理
├── /admin/users      用户管理
├── /admin/leases     租约记录
├── /admin/logs       系统日志
└── /admin/settings   系统参数 + 自动化健康检查
```

### 3.2 角色 × 内容矩阵

| 内容 | 游客 | 普通用户 | 管理员 |
|---|---|---|---|
| 池统计（总数/可用/使用中/回收中） | ✅ | ✅ | ✅ |
| 账号代码 + 状态列表 | ✅ | ✅ | ✅ |
| 使用人姓名 | ❌（一律显示"使用中"） | ❌ | ✅ |
| 领取 / 归还 / 打开科应 | ❌ | ✅ | ✅ |
| 自己的账号密码 | ❌ | ✅（仅当前租约） | ✅（查看/重置） |
| 强制回收 / 重置密码 / 标记可用 / 禁用 | ❌ | ❌ | ✅ |
| 用户管理 / 租约记录 / 日志 / 设置 | ❌ | ❌ | ✅ |

> 隐私决策（对应 PRD §35）：全站默认匿名显示使用人，仅管理员视图展示姓名——第一版推荐，减少不必要的信息暴露。

### 3.3 导航结构

- **非管理员**：单页流。顶部薄 header（wordmark + 插件状态 + 登录态），无侧边栏。核心操作全部收敛在首页 CTA 与 `/my` 两个入口。
- **管理员**：App Shell = 左侧 240px 侧边栏（`#fafafa`，surface 层级）+ 右侧内容区（canvas `#f5f5f5`，内容卡片 `#ffffff`）。侧边栏项：账号管理 / 用户管理 / 租约记录 / 系统日志 / 系统参数。

---

## 4. 状态视觉语言（核心设计决策）

### 4.1 状态视觉语言：语义色标签（v1.1 已确认）

> **决策**：状态 Tag / 标识允许引入语义色。彩色**仅限状态标签这一种元素**（状态圆点与状态徽章），按钮、卡片、导航、正文、图标保持 DESIGN.md 的无彩色规范。ember 仍是 ERROR 与破坏性操作的专属色，与状态色体系同义不冲突。

账号、租约、插件、自动化四类状态共用一个视觉语法：**8px 语义色圆点 + 12px/500 彩色徽章（soft 变体）+ 文本标签**。文本仍是语义主通道（色盲安全、灰度滤镜可读），颜色是强化通道。

| 语义 | 圆点 | 徽章（soft 变体） | 文本 |
|---|---|---|---|
| 可用 | 实心 `#16a34a`（绿） | bg `#f0fdf4` / 文本 `#15803d` | 可用 |
| 使用中 | 实心 `#2563eb`（蓝） | bg `#eff6ff` / 文本 `#1d4ed8` | 使用中 |
| 回收中 | `#d97706`（琥珀）空心 + ¾ 弧旋转（SVG stroke，2s/圈） | bg `#fffbeb` / 文本 `#b45309` | 回收中 |
| 异常 | 实心 `#e7000b`（红） | bg `#fdecec` / 文本 `#e7000b` | 异常 |
| 已释放 | 空心 `#737373`（灰，静态） | outline（透明底 / hairline 描边） | 已释放 |

使用场景：Account `AVAILABLE / IN_USE / RECYCLING / ERROR`、Lease `ACTIVE / RECYCLING / RELEASE_REQUESTED / FAILED / RELEASED`、插件状态、自动化健康检查、日志结果——凡"状态"皆走此表。

> **已确认决策（v1.1）**：①「使用中 = 蓝」定案——PRD 示意图的红色与 ember 错误色语义冲突，采用蓝色。②「琥珀 = 回收中 / 插件警告」两类语义共用，二者同为"进行中 / 提醒"语义，不冲突。

规则：

- 语义色只出现在状态圆点与状态徽章上；徽章统一使用「色 50 级底 + 色 700 级文本」的 soft 变体，全站不出现实心彩色填充徽章（实心/反色保留给黑白系强调）。
- 每行最多一个彩色徽章，避免彩色噪音；正文、按钮、链接、图标不染色。
- 状态必须始终「圆点 + 文本」成对出现，去掉颜色后信息不丢失（灰度滤镜检查保持可读）。
- ember 的合法出现：Account `ERROR`、Lease `FAILED`、重置失败日志、破坏性确认弹窗中的危险文案与图标、错误 toast——除此之外红色即设计违规。
- 状态切换动画：仅回收中琥珀色圆点旋转 + 状态文本 fade 过渡（150ms）；颜色不做闪烁/呼吸动画。

### 4.2 时间表达规范

| 元素 | 格式 | 说明 |
|---|---|---|
| 倒计时 | `27:46`（mm:ss，tabular） | 全站唯一倒计时格式 |
| 相对时间 | `3 分钟前` | 显示于"最后操作"；hover 显示绝对时间 tooltip |
| 绝对时间 | `2026-08-31 10:35` | 日志、最后改密 |
| 无操作时长 | `02:14`（mm:ss，tabular） | 悬浮窗"无操作"行 |
| 阈值标记 | 25 分钟 → 警告态；29 分钟 → 临界弹窗 | 阈值由后端 `system_settings` 下发 |

### 4.3 密码展示规范（对应 PRD §36）

- 密码默认遮蔽：`••••••••••••`（等宽点）。
- `显示` 按钮：点击后显示明文，**30 秒后自动重新遮蔽**；切路由/切 Tab 立即遮蔽。
- `复制` 按钮：复制到剪贴板 + 按钮短暂变为"已复制 ✓"（6 秒）+ 状态恢复。
- 遮蔽/显示状态在按钮文本上同步（显示 ⇄ 隐藏），不使用纯图标。
- 复制/显示动作计入审计日志（action 不包含密码本体）。

### 4.4 插件状态表达（对应 PRD §54）

| 状态 | 视觉 | 行为 |
|---|---|---|
| 正常 | 绿点 + 绿 soft 徽章：`助手已就绪 · 1.0.0` | 领取可用 |
| 未安装 | 琥珀点 + 琥珀 soft 徽章：`未检测到助手` + 链接 `下载插件 / 安装教程` | 领取按钮禁用，tooltip 说明原因 |
| 版本过旧 | 琥珀点 + 琥珀 soft 徽章：`版本过旧 1.0.0 → 最低 1.1.0` + `下载最新版` | 领取按钮禁用 |

> 未安装/过旧 = 警告语义 → 琥珀色（与回收中同款）；ember 仍保留给系统错误与破坏性操作。

---

## 5. 页面设计规格

通用页面骨架：`max-width 1280px`，页边距 24–48px，卡片间距 20px。所有页面必须设计**加载态（骨架）、空态、错误态**三种非正常态。

---

### 5.1 首页 `/`（公开账号池）

**目标**：游客 3 秒看懂账号池；登录用户可一键领取。

**布局线框**：

```text
┌──────────────────────────────────────────────────────────────┐
│ 科应共享账号                                     [登录]       │  ← 薄 header
│                                                              │
│  可用 6    使用中 3    回收中 1    异常 0                      │  ← Stat Block 行
│                                                              │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ 账号池                                        [我要使用科应] │  ← Card 24px
│  │                                                        │  │
│  │  KY-01     ○ 可用                                      │  │
│  │  KY-02     ● 使用中        预计释放 28:04               │  │
│  │  KY-03     ◐ 回收中                                    │  │
│  │  KY-04     ● 使用中        预计释放 11:20               │  │
│  │  KY-05     ● 异常          需人工处理                   │  │  ← ember 圆点
│  │  …                                                     │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                              │
│  科应共享账号助手   ● 已就绪 1.0.0          [下载插件]         │  ← 插件状态条（绿点）
└──────────────────────────────────────────────────────────────┘
```

**组件清单**：Header / Stat Block ×4 / Card / 账号池行（自定义 RowList）/ 主按钮 / 插件状态条。

> 状态图例：`● 绿 = 可用` · `● 蓝 = 使用中` · `◐ 琥珀 = 回收中` · `● 红 = 异常`（§4.1 语义色）。

**设计要点**：

- Stat Block 严格按 DESIGN.md：label 12px 大写 `#737373`，value 36px/600 `#0a0a0a`、-0.025em 紧排、tabular-nums。四个统计并排，不使用卡片包裹（纯排版层级）。
- 账号池行 = 左（状态圆点 + `KY-01` 等宽感代码字 14px/500）+ 中（状态徽章 + 使用人匿名文本）+ 右（IN_USE 行显示 `预计释放 mm:ss`；ERROR 行显示 `需人工处理` + ember 文案；其余行空）。
- CTA 规则：游客 → 显示 `[登录后使用]`（outline）；登录但插件未就绪 → 主按钮禁用 + 下方插件状态条给出原因；登录且就绪 → `[我要使用科应]` 主按钮。
- 已登录且有活动租约 → 首页 CTA 变为 `[查看我的账号 →]`（ghost），直通 `/my`。
- 账号池每 10 秒轮询刷新统计与行状态（静默更新，无加载闪烁；行状态变化用 150ms fade）。
- 空态（无任何账号）：卡片内 caption 文本 `暂无科应账号，请联系管理员录入`。

---

### 5.2 登录 `/login`

**目标**：轻量本地身份登录，12 小时会话（PRD §5）。

**布局线框**：

```text
┌──────────────────────────────────────────────────────────────┐
│                                                              │
│                    ┌────────────────────────┐                │
│                    │   科应共享账号            │  ← wordmark 30px/600
│                    │                        │                │
│                    │ 用户名                  │                │
│                    │ ┌────────────────────┐ │                │
│                    │ │                    │ │  ← input 18px 填充面
│                    │ └────────────────────┘ │                │
│                    │ 密码                    │                │
│                    │ ┌────────────────────┐ │                │
│                    │ │ ••••••••     显示   │ │                │
│                    │ └────────────────────┘ │                │
│                    │                        │                │
│                    │   [      登  录      ]  │  ← 实心主按钮
│                    │                        │                │
│                    │  用户名或密码错误        │  ← 错误态：ember 文案
│                    │  仅限内部员工使用        │  ← caption
│                    └────────────────────────┘                │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**组件清单**：Card 24px（居中，宽 400px）/ FormField（label 12px 500 + input）/ 实心主按钮（通栏）/ 密码可见切换。

**设计要点**：

- 页面无插图、无品牌色块——纯卡片 + 排版，符合"界面即组件"的规范气质。
- 登录失败：输入框上方 ember 文本 `用户名或密码错误`（不区分"用户不存在"与"密码错误"，避免账号枚举），不弹 toast。
- 登录成功 → 直接进入领取流程检查（插件检测 → 领取），不落在空首页。
- 已登录访问 `/login` → 重定向 `/`。
- 记住我/忘记密码：第一版不做（管理员重置登录密码，见 §5.6）。

---

### 5.3 我的账号 `/my`

**目标**：展示当前租约全貌：账号、密码、倒计时、两个动作（打开科应 / 立即归还）。无租约时提供领取入口。

**布局线框（有租约）**：

```text
┌──────────────────────────────────────────────────────────────┐
│ 我的账号                                     [张三 · 退出]    │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ 已分配科应账号                                          │  │
│  │                                                        │  │
│  │  KY-03                                                  │  │  ← 36px/600
│  │  ● 使用中     助手已就绪 1.0.0                           │  │
│  │                                                        │  │
│  │  密码    ••••••••••••        [显示] [复制]               │  │
│  │                                                        │  │
│  │  [        打开科应        ]  [       立即归还        ]    │  │
│  │                                                        │  │
│  │  最后操作    3 分钟前                                    │  │
│  │  预计自动释放  27:14                                    │  │
│  │  ────────────────────────●───────────────              │  │  ← 30 分钟进度条
│  │                                                        │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                              │
│  最近 5 条租约记录（本账号代码只显示 KY-xx，不显示其他用户）    │  ← caption 附注
└──────────────────────────────────────────────────────────────┘
```

**状态分支**：

| 场景 | 界面 |
|---|---|
| 无租约 | 空态卡片：`当前没有使用中的科应账号` + `[我要使用科应]` 主按钮 + 池统计迷你行 |
| 租约 ACTIVE | 如上完整卡片 |
| 租约已进入 RECYCLING | 卡片顶部 info 条：`账号正在回收，密码重置后即可重新领取`（outline badge），按钮禁用 |
| 租约 RELEASED | 空态 + caption `账号已于 10:32 自动释放` |
| 插件异常 | 卡片内警告条（solid badge）：`未检测到科应账号助手，将无法续期` |

**设计要点**：

- 主操作 `[打开科应]`（实心主按钮）依赖插件：由扩展完成 `BIND_AND_OPEN`（PRD §15），页面点击后若 3 秒无插件应答则降级为错误提示条。
- `[立即归还]` 为 outline 按钮；点击弹二次确认（§9.3）。
- 30 分钟进度条：hairline 底 + `#0a0a0a` 填充（使用时长占比），纯形表达，无颜色语义。
- 密码区域在租约存在时始终可见（遮蔽态），符合 PRD §36。

---

### 5.4 管理后台 App Shell

**布局线框**：

```text
┌──────────┬───────────────────────────────────────────────────┐
│ 科应共享  │  账号管理                                [admin·退出]│
│ 账号管理  │  ← breadcrumb：管理 / 账号管理                     │
│          │                                                  │
│ ● 账号管理│  ┌────────────────────────────────────────────┐   │
│ ● 用户管理│  │ （页面内容卡片）                              │   │
│ ● 租约记录│  │                                            │   │
│ ● 系统日志│  └────────────────────────────────────────────┘   │
│ ● 系统参数│                                                  │
│          │                                                  │
│ 侧边栏    │                                                  │
│ #fafafa   │  内容区 canvas #f5f5f5                            │
│ 240px     │                                                  │
└──────────┴───────────────────────────────────────────────────┘
```

**规范**：

- 侧边栏：`#fafafa` 背景、无边框（surface 层级自然区分）；当前项 = 实心圆点 + ink 文本，未选中 = mid-gray 文本；hover = soft 面。底部固定 `v1.0.0` caption。
- 顶栏：hairline 底边，左侧 breadcrumb（DESIGN.md Breadcrumb Trail 规范），右侧当前管理员 + `退出`。
- 非管理员访问 `/admin/*`：403 空态卡片（`无访问权限` + `[返回首页]`）。
- 所有管理页面共用 DataTable 规范（§7.5）。

---

### 5.5 `/admin/accounts` 账号管理

**目标**：10 个科应账号的运维台（PRD §37 账号管理 + §47 错误处理）。

**布局线框**：

```text
┌──────────────────────────────────────────────────────────────┐
│ 科应账号管理                                        [新增账号] │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ 账号      状态      使用者      最后改密     操作          │ │
│ │ ───────────────────────────────────────────────────────  │ │
│ │ KY-01     ○ 可用    —           07-31 09:00  [重置密码]   │ │
│ │ KY-02     ● 使用中  张三        08-01 14:12  [强制回收]   │ │
│ │ KY-03     ◐ 回收中  —           —            —           │ │
│ │ KY-04     ● 异常    —           08-02 09:00  [重试]       │ │
│ │                                              [人工处理]  │ │
│ │ …                                                       │ │
│ └──────────────────────────────────────────────────────────┘ │
│                                                              │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ ⚠ 自动回收失败 · KY-04                                    │ │  ← 错误卡片
│ │ 错误：未找到"重置密码"按钮（08-02 09:15）                   │ │
│ │                             [重试]  [人工处理完成]         │ │
│ └──────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

**设计要点**：

- 行操作列：`强制回收`（ember 文案 + outline 按钮）→ 二次确认；`重置密码`（ghost）→ 确认弹窗（手动改密 = 立即踢出当前会话）；`标记可用`（仅 ERROR 行可用，ghost）；`禁用账号`（ember 文本按钮）。
- **ERROR 账号置顶卡片化**：回收失败不藏在表格里，单独错误卡片（如上），含错误信息 + `[重试]`（调用 Playwright 重跑，PRD §48 最多 2–3 次）与 `[人工处理完成]`。
- RECYCLING 行操作列置灰 `—`，避免并发操作。
- 账号代码用等宽数字感排版（Geist 500，14px）。
- 状态列使用 §4.1 语义色体系（绿/蓝/琥珀/红 soft 徽章）；禁用账号整行 opacity-50。

---

### 5.6 `/admin/users` 用户管理

**布局线框**：

```text
┌──────────────────────────────────────────────────────────────┐
│ 用户管理                                           [创建用户]  │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ 用户名    姓名    部门      角色     状态    操作          │ │
│ │ ───────────────────────────────────────────────────────  │ │
│ │ zhangsan  张三   研发部    USER    ● 启用   [编辑][禁用]  │ │
│ │ lisi      李四   产品部    USER    ○ 停用   [编辑][启用]  │ │
│ │ admin     管理员  IT       ADMIN   ● 启用   [编辑]        │ │
│ └──────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

**设计要点**：

- `创建用户` / `编辑` → 对话框（Dialog 24px）：用户名、姓名、部门、角色（USER/ADMIN 单选）、初始密码（仅创建时出现，输入框带"生成随机密码"辅助）。
- `重置登录密码` → 弹窗内输入新密码 + 确认，成功后 toast `已重置，请转告用户`。
- 禁用用户：二次确认；被禁用用户的活动租约**提示管理员先强制回收**（弹窗内说明）。
- 不允许管理员禁用/降级自己（后端规则，前端置灰）。

---

### 5.7 `/admin/leases` 租约记录

**布局线框**：

```text
┌──────────────────────────────────────────────────────────────┐
│ 租约记录                                      筛选：[全部▾]   │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ 领取人   账号    领取时间      最后操作      释放时间   原因 │ │
│ │ ───────────────────────────────────────────────────────  │ │
│ │ 张三    KY-02   08-01 14:00   08-01 15:30   08-01 15:31  │ │
│ │        ● 使用中                                        TIMEOUT│
│ │ 李四    KY-01   07-31 09:00   07-31 11:00   07-31 11:02  │ │
│ │                                                       USER_RETURN│
│ │ …                                                       │ │
│ └──────────────────────────────────────────────────────────┘ │
│ 共 47 条 · 上一页 下一页                                       │
└──────────────────────────────────────────────────────────────┘
```

**设计要点**：

- 列：领取人 / 科应账号 / 领取时间 / 最后操作 / 释放时间 / 释放原因（PRD §37）。
- 释放原因映射为徽章：`USER_RETURN` outline（灰）、`TIMEOUT` 琥珀 soft、`ADMIN_FORCE` 蓝 soft、`RESET_ERROR` ember soft。
- 筛选：状态（全部/使用中/已释放）+ 账号代码搜索；分页 20 条/页。
- 活动租约行（未释放）高亮：左侧 2px ink 竖条，释放时间列显示 `—`。

---

### 5.8 `/admin/logs` 系统日志

**布局线框**：

```text
┌──────────────────────────────────────────────────────────────┐
│ 系统日志                                       动作：[全部▾]   │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ 时间                动作            结果   用户      IP     │ │
│ │ ───────────────────────────────────────────────────────  │ │
│ │ 08-02 09:15:03   RESET_FAILED    ● 失败  系统      —      │ │
│ │ 08-02 09:14:50   RESET_SUCCESS   ○ 成功  系统      —      │ │
│ │ 08-02 09:10:22   CLAIM_ACCOUNT   ○ 成功  张三  10.2.1.8  │ │
│ │ 08-02 09:10:20   LOGIN           ○ 成功  张三  10.2.1.8  │ │
│ │ …                                                       │ │
│ └──────────────────────────────────────────────────────────┘ │
│ 共 2,318 条 · 上一页 下一页                                    │
└──────────────────────────────────────────────────────────────┘
```

**设计要点**：

- 动作清单按 PRD §58（LOGIN / CLAIM_ACCOUNT / ACTIVITY / RELEASE / TIMEOUT / RESET_PASSWORD / RESET_SUCCESS / RESET_FAILED / ADMIN_FORCE_RELEASE / ADMIN_MANUAL_FIX）。
- `结果` 列：成功 = soft badge `成功`；失败 = ember outline `失败`；进行中 = outline `进行中`。
- `ACTIVITY` 日志量大会刷屏：**默认隐藏**，提供开关 `显示 Activity 明细`（caption + 开关），默认关。
- 行 hover 显示 metadata 详情（tooltip，禁止包含密码）。
- 分页 20 条/页 + 动作筛选下拉。

---

### 5.9 `/admin/settings` 系统参数

**布局线框**：

```text
┌──────────────────────────────────────────────────────────────┐
│ 系统参数                                                      │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ 租约规则                                                  │ │
│ │ 无操作超时（秒）          [1800]      应用                │ │
│ │ 即将释放提醒（秒）        [300]       应用                │ │
│ │ 临界提醒（秒）            [60]        应用                │ │
│ │ 保存                                                      │ │
│ └──────────────────────────────────────────────────────────┘ │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ 扩展配置                                                  │ │
│ │ 最低版本  1.0.0      最新版本  1.2.0      [下载最新版 ZIP]  │ │
│ └──────────────────────────────────────────────────────────┘ │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ Scienceing 自动化                                         │ │
│ │ ● 管理员登录正常      ● 账号管理页可访问      ● 改密入口正常 │ │
│ │ 最后检测：2026-08-31 10:35                    [立即检测]   │ │
│ └──────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

**设计要点**：

- 三张卡片：租约规则（数字输入 + `应用`，改动即时生效并写 audit_log）、扩展配置（版本号 + 下载链接，对应 PRD §11）、自动化健康检查（PRD §49：三项检查逐项状态 + 最后检测时间 + `[立即检测]`）。
- 健康检查结果：三项全部通过 → 绿 soft 徽章 `正常`；任一失败 → ember soft + 失败项文本。
- 数字输入：右对齐 tabular、`秒` 后缀 caption。

---

### 5.10 插件安装引导（弹窗）

**目标**：首次使用引导用户安装扩展（PRD §53）。

**布局线框**：

```text
┌──────────────────────────────────────────────────────────────┐
│ 安装科应共享账号助手                                           │
│                                                              │
│ 1  下载 ZIP                [下载插件 v1.2.0]                  │
│ 2  解压到固定目录           C:\ScienceingAssistant\           │
│ 3  Edge: edge://extensions  Chrome: chrome://extensions      │
│ 4  开启「开发人员模式」                                        │
│ 5  「加载已解压的扩展程序」→ 选择上述目录                       │
│ 6  返回账号看板，页面将自动检测插件                              │
│                                                              │
│                     [知道了]        [打开安装教程]             │
└──────────────────────────────────────────────────────────────┘
```

**设计要点**：

- 步骤用数字圆（18px 圆、soft 面、tabular 数字）+ 文本，竖排列表。
- 浏览器路径用等宽感排版（Geist 500），可一键复制。
- 首页插件状态条与升级拦截共用此弹窗（升级场景标题变为 `升级科应共享账号助手`）。

---

## 6. 组件规格与 shadcn/ui 映射

### 6.1 组件映射表

| 组件 | shadcn 源 | 定制点 |
|---|---|---|
| 按钮（主/ghost/outline） | `Button` | 按 DESIGN.md 三变体；危险操作 = outline + ember 文本图标 |
| 卡片 | `Card` | `rounded-[24px]` + `--shadow-subtle` |
| 输入框 | `Input` | 休止填充 `#f5f5f5`、聚焦 1px hairline ring、18px |
| 表单字段 | `Label` + `Input` | label 12px/500；错误说明 12px ember |
| 徽章 | `Badge` | 黑白四变体（solid / soft / outline / ember-outline）+ §4.1 语义色 soft 变体（绿/蓝/琥珀/红） |
| 对话框 | `Dialog` | 24px 圆角；破坏性确认标题栏含 ember 图标 |
| 数据表格 | `Table` | 表头 12px/500 `#737373`、行分隔 hairline、hover soft 面 |
| 下拉选择 | `Select` | 18px、`#f5f5f5` 填充 |
| 开关 | `Switch` | ink 轨道 + paper 滑块（唯一"彩色感"元素，仍为黑白） |
| 空态 | 自定义 | 居中 caption + 图标（thin-stroke）+ outline 按钮 |
| Toast | `Sonner` / `Toast` | 成功/信息黑白；错误 ember 图标；6 秒自动消失 |
| 骨架屏 | `Skeleton` | `#f5f5f5` 底、淡脉冲 |

### 6.2 定制组件（业务专用，需自行实现）

| 组件 | 说明 |
|---|---|
| `StatusDot` | 8px 语义色圆点 + 状态文本，四种状态（§4.1），回收中带琥珀色 SVG 弧旋转 |
| `StatBlock` | DESIGN.md Stat Block：label 12px 大写 + value 30–36px/600 tabular |
| `Countdown` | mm:ss tabular-nums；每秒 tick；到 0 触发状态切换回调 |
| `AccountRow` | 首页账号池行（圆点 + 代码 + 徽章 + 预计释放） |
| `PasswordReveal` | 遮蔽/显示/复制一体组件（§4.3），30 秒自动遮蔽 |
| `PluginChip` | 看板插件状态条（就绪/未安装/过旧 + 下载入口） |
| `ProgressHairline` | 30 分钟使用进度细条（hairline 底 + ink 填充） |
| `ErrorCard` | 回收失败错误卡片（账号代码 + 错误信息 + 重试/人工处理） |
| `StepsGuide` | 安装引导步骤列表（数字圆 + 文本 + 复制路径） |

### 6.3 图标

- Lucide（thin-stroke，1.5–2px）：`KeyRound`（密码）、`Copy`、`Eye/EyeOff`、`ExternalLink`（打开科应）、`RefreshCw`（重试）、`TriangleAlert`（警告/错误，ember）、`Clock`、`Download`、`LogOut`。
- 图标一律 16px，颜色 ink / mid-gray；ember 图标仅用于错误与破坏性确认。

---

## 7. 扩展悬浮窗设计（Shadow DOM）

### 7.1 原则

- 注入科应页面右下角，Shadow DOM 根节点 `#__scienceing_account_assistant__`（PRD §44），**自带样式，不受科应 CSS 污染，也不污染科应**。
- 视觉与主站同源（同一令牌），但因不能依赖 Tailwind 运行时，将所需令牌以 CSS 变量形式内嵌于注入的 `<style>`（十六进制值 + 系统字体栈 + tabular-nums）。
- 悬浮窗内一切交互**不产生 Activity**（PRD §44），事件监听器排除插件 UI 区域。

### 7.2 尺寸与形态

- 展开态：宽 280px，右下角 fixed，距边缘 16px；白底、24px 圆角、1px hairline、`--shadow-subtle`。
- 折叠态：单行胶囊（高 36px，18px 圆角）：`● KY-03 · 27:46` + 展开箭头。
- 拖拽：第一版不做（固定右下角）；可折叠是唯一位置交互。

### 7.3 状态清单

```text
① 正常（使用中）—— 蓝点
┌──────────────────────────┐
│ ● 科应共享账号       [−]  │  ← 折叠按钮
│ KY-03                     │  ← 16px/600
│ 使用中                     │  ← 12px soft 徽章
│ ───────────────────────── │
│ 无操作      02:14         │  ← tabular
│ 预计释放    27:46         │
│ ▓▓▓▓▓▓▓░░░░░░░░░░        │  ← ProgressHairline
│            [立即归还]     │  ← 紧凑主按钮 32px
└──────────────────────────┘

② 警告（≥25 分钟无操作，PRD §21.1）—— 琥珀色，不弹 Modal
┌──────────────────────────┐
│ ⚠ 已连续 25 分钟无操作     │  ← 琥珀 soft 徽章
│ 04:59 后自动释放           │  ← tabular 18px/600
│ 继续操作科应页面即可保持    │  ← caption
└──────────────────────────┘

③ 临界（≥29 分钟，PRD §21.2）—— 琥珀色，弹一次 Modal（Shadow DOM 内）
┌────────────────────────────────┐
│ ⚠ 科应账号即将自动释放           │
│ 已连续 29 分钟无操作             │
│ 00:59 后自动释放                 │
│ 继续操作页面即可保持使用。        │
│                  [立即归还]      │
└────────────────────────────────┘

④ 已释放 —— 灰点（outline）
┌──────────────────────────┐
│ ● 账号已自动释放           │
│ KY-03 已重新进入账号池     │
│            [返回看板]     │
└──────────────────────────┘

⑤ 服务连接异常（PRD §45）—— 琥珀点
┌──────────────────────────┐
│ ⚠ 账号管理服务连接异常     │
│ 已有租约不受影响，恢复后    │
│ 将按最后活动时间计算        │
└──────────────────────────┘
```

> 色彩：使用 §4.1 语义色（① 蓝点 · ②③⑤ 琥珀 · ④ 灰）；错误场景（如 Activity 上报被拒）显示 ember。

### 7.4 悬浮窗行为规则

| 规则 | 说明 |
|---|---|
| 倒计时数据源 | 仅来自后端 `GET /leases/{id}/status` 的 `expiresAt`，插件本地只做渲染；服务器失联时显示 ⑤ 并冻结本地倒计时 |
| 续期 | 只有科应页面真实操作（isTrusted）触发；操作悬浮窗/弹窗**不续期** |
| 折叠记忆 | 折叠状态存 `chrome.storage.session`（会话级，不落盘隐私） |
| 多 Tab | 所有继承同一 Lease 的 Tab 共享同一悬浮窗实例（由 Worker 管理，PRD §16） |
| 未绑定 Tab | 不注入悬浮窗；看板打开流程保证绑定（PRD §15），绑定失败时页面内提示 `请从账号看板重新打开` |
| 中文字体 | 系统 CJK 回退，不加载字体文件（扩展体积与权限最小化） |

---

## 8. 关键交互流程设计

### 8.1 领取流程（对应 PRD §12 / §51）

```text
点击 [我要使用科应]
  → 已登录？否 → /login → 成功 → 继续
  → 插件检测（3 秒超时）
      ├─ 未安装    → 安装引导弹窗（§5.10），CTA 禁用
      ├─ 版本过旧  → 升级弹窗，CTA 禁用
      └─ 就绪      ↓
  → 已有活动租约？是 → 跳转 /my（页面即显示当前账号）
  → 有可用账号？否 → 空态卡片：`暂无可用账号，使用中 3 · 回收中 1` + 轮询刷新
  → 创建租约成功 → 跳转 /my，展示密码（遮蔽）
  → 创建失败（并发抢完）→ 错误 toast `账号刚刚被领取，请重试` + 刷新列表
```

### 8.2 归还流程（对应 PRD §32）

```text
[立即归还]（看板 /my 或悬浮窗）
  → 确认弹窗：`归还后将重置密码并退出当前会话`
      ├─ 取消 → 关闭
      └─ 确认 → 按钮进入 loading（`回收中…`）
  → 成功 → /my 显示 info 条 `账号正在回收`
  → 悬浮窗 → 状态 ① → ④（已释放）
```

### 8.3 强制回收（管理员，对应 PRD §52）

```text
[强制回收] → 确认弹窗：
  ⚠ 强制回收 KY-02
  将重置密码并立即退出张三的科应会话。
              [取消]  [确认回收]
→ 确认 → 行状态变回收中（旋转圆点）→ 完成后 toast `KY-02 已回收`
→ 失败 → 账号置顶 ErrorCard（§5.5）
```

### 8.4 自动释放的界面行为（PRD §26）

```text
30 分钟无操作
  → 后端 ACTIVE→RECYCLING → Playwright 改密
  → 悬浮窗：倒计时到 0 瞬间显示 ④ `账号已自动释放`
  → 科应页面被踢回登录页（科应自身行为）
  → 看板：该行变 `回收中`，随后变 `可用`
  → /my：租约 RELEASED 空态
```

### 8.5 全局错误与降级

| 场景 | 界面行为 |
|---|---|
| 后端不可用（看板） | 全页错误卡：`服务不可用` + `[重试]`；禁领取（PRD §45） |
| 后端不可用（插件） | 悬浮窗状态 ⑤，本地倒计时冻结，不踢已有租约 |
| 插件被关闭 | 看板插件条变未安装态，禁领取；已有租约 30 分钟后自然回收（PRD §46） |
| 领取时并发冲突 | 错误 toast + 列表自动刷新（PRD §60 场景 10） |
| Playwright 失败 | ERROR 账号置顶 ErrorCard，永不自动转 AVAILABLE（PRD §47） |

---

## 9. 响应式、可访问性与动效

### 9.1 响应式

- 桌面优先（内网工具）：断点 1280（内容 max-width）/ 1024 / 768。
- `<1024`：管理后台侧边栏收为顶部 Tab 条；表格横向滚动（表头冻结首列账号代码）。
- `<768`：首页统计两列折行；账号池行右列时间隐藏为 tooltip；悬浮窗尺寸不变（独立于页面布局）。

### 9.2 可访问性

| 项 | 规范 |
|---|---|
| 状态可读性 | 所有状态 = 圆点 + 文本，无颜色依赖（WCAG 1.4.1） |
| 对比度 | 正文 ink/mid-gray 在 paper/canvas 上 ≥ 4.5:1；caption 12px 仅用于非关键信息 |
| 键盘 | 全站 Tab 可达；确认弹窗焦点陷阱 + Esc 关闭 + 焦点还原 |
| Focus | `focus-visible`：2px `#0a0a0a` 环 + 2px offset（不用模糊 glow） |
| 倒计时 | 每秒 tick 不触发 aria-live；到 25/29 分钟阈值与释放瞬间才播报（aria-live=polite） |
| 表单 | 每个输入关联 label；错误文案关联 `aria-describedby` |
| 动效降级 | `prefers-reduced-motion`：关闭旋转/脉冲/过渡，保留状态文本切换 |

### 9.3 动效清单（全部克制，无 GSAP 级动画）

| 元素 | 动效 |
|---|---|
| 状态切换 | 150ms opacity/translateY(2px) fade |
| 回收中圆点 | SVG ¾ 弧 2s 匀速旋转（reduced-motion 时静止） |
| 按钮 | hover：主按钮 `#0a0a0a→#171717`；ghost/outline hover 面 `#fafafa`；active 按压 1px 位移（不缩放） |
| 倒计时 | 无动画，tabular-nums 保证不抖动 |
| 骨架屏 | 1.2s 淡脉冲（reduced-motion 时静态灰块） |
| 弹窗 | 120ms 淡入 + scale(0.98→1) |

---

## 10. 设计验收清单（对应 PRD §60 十个场景）

> 实现完成后逐条人工验收，全部通过视为设计达标。

| # | 场景 | 界面验收点 |
|---|---|---|
| 1 | 正常领取 | 首页 CTA → 登录 → 领取成功落地 `/my`，密码遮蔽显示；另一用户首页该行显示 `使用中` |
| 2 | 一人一租约 | 已有租约用户再点 CTA 直接进 `/my`，不出现第二个账号 |
| 3 | 插件检测 | 插件关闭时首页 CTA 禁用 + 状态条 `未检测到助手`；3 秒判定 |
| 4 | 正常 Activity | 悬浮窗与 `/my` 倒计时在真实操作后回到 ~30:00；无操作时倒计时持续递减 |
| 5 | 页面挂机 | 倒计时到 0 → 悬浮窗 `已释放` 态；看板行变 `回收中→可用` |
| 6 | 浏览器关闭 | 重开看板：该行 `回收中`；无残留"使用中"行 |
| 7 | 主动归还 | `/my` 与悬浮窗 `立即归还` → 确认 → 回收中 → 释放后空态提示 |
| 8 | 自动回收 | 释放后看板行 `可用`，他人可领取；ERROR 场景见 9 |
| 9 | 改密失败 | `/admin/accounts` 出现置顶 ErrorCard，行状态 `异常`（ember），无自动转可用 |
| 10 | 并发领取 | 双端同时领取：一方成功落地 `/my`，另一方错误 toast + 列表刷新 |

### 附加设计验收

- 全站截图检查：**彩色仅出现在状态标签（§4.1 语义色表）与 ERROR/破坏性确认中**；按钮、卡片、导航、正文无彩色；无渐变、无彩色阴影、无第三种圆角。
- 用灰度滤镜（`filter: grayscale(1)`）检查所有页面：状态仍可读。
- 断点 1280/1024/768 截图检查无溢出。
- 键盘走查：领取→归还→管理端全流程无鼠标可达。

---

## 11. 分阶段落地计划（与 PRD §59 对齐）

| 阶段 | 设计交付 | 对应 PRD 阶段 |
|---|---|---|
| **Phase 0 · 设计系统落地** | theme.css 引入、shadcn 变量映射（§2.2）、Geist 字体、`StatusDot/StatBlock/Countdown/PasswordReveal/PluginChip` 五个定制组件、App Shell 布局 | — |
| **Phase 1 · 账号池** | 首页、登录、`/my`、admin 五个页面（账号/用户/租约/日志/设置）全部完成并过验收清单 1/2/7/10 | Phase 1 |
| **Phase 2 · 扩展 UI** | 安装引导弹窗、升级拦截、插件状态条、悬浮窗五态（§7.3）、Activity 上报 UI 反馈；验收 3/4/5/6 | Phase 2 |
| **Phase 3 · 自动化 UI** | ErrorCard、健康检查卡片、`重试`/`人工处理完成` 交互、RESET 日志状态；验收 9 | Phase 3 |
| **Phase 4 · 闭环打磨** | 全链路状态流转走查（§8.4）、动效与可访问性终检、灰度滤镜检查、设计验收清单全绿 | Phase 4 |

**每阶段出口标准**：对应验收清单场景全绿 + 设计走查（§10 附加验收）通过。

---

## 12. 风险与待确认

| # | 风险 / 待确认 | 建议 | 责任人 |
|---|---|---|---|
| 1 | ~~状态是否彩色化~~ **已确认（v1.1）**：状态标签引入语义色（绿/蓝/琥珀/红），令牌已入 `theme.css`，边界见 §4.1；剩余执行风险：评审与实现时防止彩色越界进入其他元素 | 以 §4.1 语义色表为唯一依据做代码评审 | 设计 + 开发 |
| 2 | 中文显示级标题（48px）视觉过重 | 中文标题上限 36px；大数字（统计）可 48px | 设计 |
| 3 | 科应改密通知（PRD §50）可能刷屏 | 非界面问题，运营确认；若确认，管理端可在账号行加 `通知已发` caption | 产品 |
| 4 | 悬浮窗在科应页面内的可发现性 | 首次绑定后 10 秒内悬浮窗高亮描边一次（solid 徽章），随后静默 | 设计 |
| 5 | 表格数据量增长（日志/租约） | 第一版分页 + 筛选足够；超万条再议虚拟滚动 | 开发 |
| 6 | 游客页暴露账号代码的敏感性 | 按 PRD §35 保留代码列表；如敏感可降级为只显示统计（一行改动，设计已支持） | 产品 |

---

## 13. 评审后动工清单

1. 评审确认 §4 状态视觉语言（v1.1：状态标签已确认引入语义色，边界见 §4.1）；
2. 确认 §3.2 匿名显示使用人的隐私决策；
3. 确认 §11 Phase 0 定制组件清单；
4. 按 Phase 0 → Phase 4 顺序排期开发；
5. 每阶段末执行 §10 验收清单。
